import React, { useEffect, useState, useRef } from "react";
import "./style.css";

const HSeperator = ({ label, className }) => {
  return <div className={`ui-h-seperator ${className}`}></div>;
};

export default HSeperator;
